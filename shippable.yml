language: scala

scala:
  - 2.13

build:
  ci:
    - SBT_OPTS='-Xmx1536m'; sbt ";clean ;coverage ;test ;coverageReport ;coverageAggregate"
    - cp target/scala-2.13/coverage-report/cobertura.xml shippable/codecoverage

jdk:
  - oraclejdk8